// run initial animations 
function init() {
  // const cards = document.getElementsByClassName('project-card')
  // for (let i=0; i<cards.length; i++) {
  //   cards[i].classList.add('tilt-in-top-1')
  // }
  // setTimeout(() => {
  //   for (let i = 0; i<cards.length; i++) {
  //     cards[i].classList.remove('tilt-in-top-1')
  //   }
  // }, 2000)
  // for (let i=0; i<cards.length; i++) {
  //   cards[i].addEventListener('mouseenter', () => cards[i].classList.add('jello-horizontal'))
  // }
}